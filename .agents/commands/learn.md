---
description: Record a learning from what we just solved.
---
Record a learning for the pitfall we just resolved, following `.agents/skills/record-learning/SKILL.md`:
write a four-block entry under `.agent-learnings/entries/` and add a line to the index
`.agent-learnings.md` under the right tag(s).

Keep it short, tag it for future retrieval, and record only durable lessons — not one-off noise.
