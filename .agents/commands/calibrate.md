---
description: Guided one-time setup for this project (fills AGENTS.md + initial rubric weights).
---
Set this project up by following `.agents/skills/onboarding/SKILL.md`.

First, if the template has not been bootstrapped yet, run `scripts/setup.sh` (or `scripts/setup.ps1`
on Windows). Then interview me a few questions at a time, **draft** the `AGENTS.md`
Project / Setup / Constraints sections and the initial rubric weights, and write them only after I
confirm.

Touch the soft layer only — never weaken the tests, the scope rules, or the binary gates. When the
setup is confirmed, suggest `/plan` to define the first phase.
