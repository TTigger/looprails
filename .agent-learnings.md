# Agent Learnings (index)

<!--
RETRIEVAL: plain Markdown + tags (ADR-006). Agents read THIS index first, then open only the
entries matching a task's tags. Entries live in .agent-learnings/entries/ (one pitfall per file).

UPGRADE PATH (non-breaking): when entries exceed ~150 or retrieval starts missing, add a CLI that
builds a semantic index (SQLite FTS / embeddings). The underlying Markdown does not change.

Add lines under a tag heading like:
  - [Title](.agent-learnings/entries/2026-06-04-slug.md) — one-line summary
Mark superseded lines [DEPRECATED] instead of deleting.
-->

## #general

## #setup
