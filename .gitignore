# dependencies / build
node_modules/
dist/
build/
.next/
*.log

# secrets / env
.env
.env.*
!.env.example

# loop runtime artifacts (regenerated; NOT a source of truth).
# NOTE: gitignore has no inline comments — keep notes on their own lines or they become part of the pattern.
.active-scope
.review/
.worktrees/
# screenshot cache for pairwise scoring
.agent-learnings/_best/
# generated by bootstrap from .agents/commands/ (single source of truth lives there)
.claude/commands/

# NOTE: .agent-learnings/entries/ IS tracked on purpose — it is the memory. Do not ignore it.
# tasks/ is generated from PHASES.md; commit it for traceability, or add `tasks/` here if you
# prefer to treat it as ephemeral.

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
